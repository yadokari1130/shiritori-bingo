# しりとりビンゴ 開発指示

このプロジェクトの実装・レビュー・テストでは、仕様書を実装判断の基準とすること。

## 仕様書の参照

- プロジェクト全体の概要を確認する場合は、`.agents/rules/outline.md`を読むこと。
- 仕様書の構成と各章への参照を確認する場合は、`.agents/rules/specification.md`を読むこと。
- 実装対象に関係する章を、`.agents/rules/spec-docs/`から必要に応じて読むこと。
- ゲームルール、画面の挙動、データ構造、非機能要件が関係する変更では、該当する章を確認してから作業すること。
- 仕様書に記載されていない挙動を独自に追加せず、既存の仕様との矛盾がある場合は仕様書を優先して確認すること。

## プロジェクトの基本方針

- ブラウザで動作するフロントエンドと、ルーム管理・ゲーム進行を担当するバックエンドで構成すること。
- 外部API、外部辞書サービス、外部CDNは使用しないこと。ルーム同期に必要なバックエンドとの通信は行うこと。
- ブラウザの`localStorage`には名前付き設定プリセットを保存し、ゲーム進行状況はサーバーのSQLiteで永続化すること。
- プレイヤー名や入力単語は、HTMLとして解釈せず安全に表示すること。
- バックエンドのテスト実行時は、`backend` ディレクトリにて `uv run pytest` を使用すること。
- バックエンドのコード品質確認・リント実行時は、`backend` ディレクトリにて `uv run ruff check .` を使用し、コード修正後は必ず実行してエラーがないことを確認すること。
- フロントエンドのテスト実行時は、`frontend` ディレクトリにて `pnpm test` を使用すること。
- フロントエンドの型チェック実行時は、`frontend` ディレクトリにて `pnpm typecheck` を使用すること。
- フロントエンドのコード品質確認・リント実行時は、`frontend` ディレクトリにて `pnpm lint` または `pnpm lint:fix` を使用し、コード修正後は必ず実行してエラーがないことを確認すること。

